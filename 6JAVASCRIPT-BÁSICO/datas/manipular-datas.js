// Alguns métodos para manipular Datas.

getDate() // Retorna o dia do mês (de 1 a 31).
getDay() // Retorna o dia da semana (de 0 a 6).
getFullYear() // Retorna o ano.
getHours() // Retorna a hora (de 0-23).
getMilliseconds() // Retorna os milissegundos (de 0-999).
getMinutes() // Retorna os minutos (de 0-59).
getMonth() // Retorna o mês (de 0 a 11).
getSeconds() // Retorna os segundos (de 0-59).
getTime() // Retorna o número de milissegundos desde a meia-noite de 1º de janeiro de 1970 e uma data especificada.
getTimezoneOffset() // Retorna a diferença horária entre a hora UTC e a hora local, em minutos.
getUTCDate() // Retorna o dia do mês, de acordo com a hora universal (de 1 a 31).
getUTCDay() // Retorna o dia da semana, de acordo com o horário universal (de 0 a 6).
getUTCFullYear() // Retorna o ano, de acordo com o horário universal.
getUTCHours() // Retorna a hora, de acordo com a hora universal (de 0 a 23).
getUTCMilliseconds() // Retorna os milissegundos, de acordo com o tempo universal (de 0-999).
getUTCMinutes() // Devolve os minutos, de acordo com a hora universal (de 0-59).
getUTCMonth() // Retorna o mês, de acordo com a hora universal (de 0 a 11).
getUTCSeconds()	// Retorna os segundos, de acordo com o tempo universal (de 0-59).
getYear() // Descontinuada. Use o método getFullYear() em vez disso.
now() // Retorna o número de milissegundos desde a meia-noite de 1º de janeiro de 1970.
parse()	// Analisa uma string de data e retorna o número de milissegundos desde 1º de janeiro de 1970.

setDate() // Define o dia do mês de um objeto de data.
setFullYear() // Define o ano de um objeto de data.
setHours()	// Define a hora de um objeto de data.
setMilliseconds() // Define os milissegundos de um objeto de data.
setMinutes() // Definir os minutos de um objeto de data.
setMonth() // Define o mês de um objeto de data.
setSeconds() // Define os segundos de um objeto de data.
setTime() // Define uma data para um número especificado de milissegundos após/antes de 1º de janeiro de 1970.
setUTCDate() // Define o dia do mês de um objeto de data, de acordo com a hora universal.
setUTCFullYear() // Define o ano de um objeto de data, de acordo com a hora universal.
setUTCHours() // Define a hora de um objeto de data, de acordo com a hora universal.
setUTCMilliseconds() // Define os milissegundos de um objeto de data, de acordo com a hora universal.
setUTCMinutes()	// Defina os minutos de um objeto de data, de acordo com a hora universal.
setUTCMonth() // Define o mês de um objeto de data, de acordo com a hora universal.
setUTCSeconds()	// Defina os segundos de um objeto de data, de acordo com a hora universal.
setYear() // Descontinuada. Em vez disso, use o método setFullYear().

toDateString() // Converte a parte de data de um objeto Date em uma string legível.
toGMTString() // Descontinuada. Use o método toUTCString() em vez disso.
toISOString() // Retorna a data como uma string, usando o padrão ISO.
toJSON() // Retorna a data como uma string, formatada como uma data JSON.
toLocaleDateString() // Retorna a parte de data de um objeto Date como uma string, usando convenções de localidade.
toLocaleTimeString() // Retorna a parte de hora de um objeto Date como uma string, usando convenções de localidade.
toLocaleString() // Converte um objeto Date em uma string, usando convenções de localidade.
toString() // Converte um objeto Date em uma string.
toTimeString() // Converte a parte de hora de um objeto Date em uma string.
toUTCString() // Converte um objeto Date em uma string, de acordo com a hora universal.
UTC() // Retorna o número de milissegundos em uma data desde a meia-noite de 1º de janeiro de 1970, de acordo com o horário UTC.
valueOf() // Retorna o valor primitivo de um objeto Date.